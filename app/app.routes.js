(function(){
	'use strict';

	angular.module('App.main', []);
		/*.config(function($routeProvider){
			## ToDo: Add proper routes

			$routeProvider
		        .otherwise({ redirectTo: '/' });
		});*/

})();